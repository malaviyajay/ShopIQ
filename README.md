# EC

## Initialize User Secret
dotnet user-secrets init

## Set Secrets

dotnet user-secrets set "Stripe:SecretKey" "sk_test_xxxxxxxxxxxxxxxxxxxxx"
dotnet user-secrets set "SmtpSettings:Password" "xxxxxxxxxxxxx"